import {
    NavigationItem,
} from "../../material/ui/items/navigation_item.slint";

// export struct Map{
//   key: string,
//   value:
// }

export global DevicesStoreSlint {
    in-out property <[string]> adapterInfos;
    in-out property <[NavigationItem]> adapterInfoNavItems;
  // in-out property <> name;
}
